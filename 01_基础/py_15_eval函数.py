# -------------------------- eval函数会把字符串中的内容当做代码执行 --------------------------
print("-" * 25, "eval函数会把字符串中的内容当做代码执行", "-" * 25)
# s = input("请输入计算内容:")
s = "1*10"
result = eval(s)

print("结果是 -> %s" % result)
# -------------------------- 注意事项 --------------------------
print("-" * 25, "注意事项", "-" * 25)
# 不要用eval直接执行input的结果，很危险

s = input("请输入计算内容：")  # 输入  __import__("os").system("ls")
print(eval(s))
